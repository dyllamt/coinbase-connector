apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
spec:
  # type: LoadBalancer
  selector:
    app: {{ .Values.appName }}
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000